# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test artifacts
test-results/
playwright-report/

# Environment variables
.env
.env.local
.env.*.local

# ข้อมูลสำรองก่อนนำเข้า (มีข้อมูลคลังจริง ห้ามขึ้น repo)
backup/

# MCP config (contains personal access token — do not commit)
.mcp.json

# Supabase CLI local state
supabase/.temp/

# Playwright MCP session artifacts (snapshots/console logs ของ browser)
.playwright-mcp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ไฟล์ข้อมูล CSV ต้นทาง (ยอดคลังยา) — ไม่เก็บใน git
csvfile/

# Local Netlify folder
.netlify

# Playwright profile ชั่วคราวสำหรับเปิด LINE Console (มี session ห้าม commit)
.line-profile/
line-webhook.png
import-linechat.mjs

# ไฟล์ CSV ของคลัง (ข้อมูลจริง) — csvfile/ ถูก ignore อยู่แล้ว แต่โฟลเดอร์นี้สะกดต่างกัน
csv_file/
.setup-profile/
open-setup.mjs
.line-profile2/
line-setup.mjs
